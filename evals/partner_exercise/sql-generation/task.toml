version = "1.0"

[metadata]
name = "sql-generation"
description = "Generate a Snowflake SQL query to identify top customers by total revenue"
category = "sql"
difficulty = "medium"
tags = ["sql", "snowflake", "partner-exercise"]

[agent]
timeout_sec = 600

[verifier]
timeout_sec = 300

[environment]
use_scratch = true
