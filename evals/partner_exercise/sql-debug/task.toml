version = "1.0"

[metadata]
name = "sql-debug"
description = "Debug and fix a broken Snowflake SQL query"
category = "sql"
difficulty = "medium"
tags = ["sql", "debugging", "snowflake", "partner-exercise"]

[agent]
timeout_sec = 600

[verifier]
timeout_sec = 300
include_dirs = ["../_shared/tests"]

[environment]
use_scratch = true
workspace_dir = "starter"
